# URL do Backend API (que fala com o Neon). Em dev pode ser /api via proxy.
VITE_API_URL=/api

# Neon Auth (Stack Auth) — chaves públicas do frontend
VITE_STACK_PROJECT_ID=
VITE_STACK_PUBLISHABLE_CLIENT_KEY=
